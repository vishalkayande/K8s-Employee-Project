apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: emp-directory
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
  # No type specified -> ClusterIP (internal only, which is correct for a DB)
