# NOTE for students: these are base64-ENCODED (not encrypted) sample values,
# purely for lab use. Decode with: echo '<value>' | base64 -d
# In real environments use a proper secret manager / sealed-secrets / Vault.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: emp-directory
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: cm9vdHBhc3MxMjM=   # rootpass123
  MYSQL_USER: YXBwdXNlcg==                 # appuser
  MYSQL_PASSWORD: YXBwcGFzc3dvcmQ=         # apppassword
  MYSQL_DATABASE: ZW1wbG95ZWVfZGI=         # employee_db
